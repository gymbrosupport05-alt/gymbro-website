- [ ] Add delegated click price parsing + console logs
- [ ] Use product dataset price from MongoDB instead of parsing rendered text
- [ ] Ensure cart stores original MongoDB `price` value
- [ ] Ensure renderCart subtotal and total use `item.price * item.quantity`

